package com.iccspace.icc_wechat_mgmt.redis;

import redis.clients.jedis.Jedis;

public class HowToTest {

    /*public static void main(String[] args) {
        Jedis jedis = null;
        try {
            jedis = new Jedis("139.196.225.220", 6379);
            jedis.auth("root");
            jedis.set("AppName", "iccsapce");
            String rs = jedis.get("AppName");
            System.out.println(rs);
        } finally {
            if (jedis != null) {
                jedis.close();
            }
        }
    }*/

}